# sentinel extensions

Place app-level helpers for the **cube-sentinel-v1** cube here.
Files in this directory are owned by the AI customization phase.
You may also create helper files directly in SDK namespaces (e.g., `src/lib/auth/`).
SDK files are overwritten on deploy; your additional files are preserved.
